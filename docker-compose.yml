version: "3.5"
services:
  frontend:
    build:
      context: frontend
      dockerfile: frontend/Dockerfile.dev
    volumes:
      - ./frontend:/app
    ports:
      - 3000:3000
  flask:
    build: flask_app
    ports:
      - 5000:5000
    volumes:
      - ./flask_app:/app
